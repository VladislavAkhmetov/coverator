# 🚀 Инструкция по деплою Ковератора на Vercel (GitHub + Vercel)

## 📋 Что нужно перед началом

- Аккаунт на GitHub (бесплатно): https://github.com/signup
- Аккаунт на Vercel (бесплатно, регистрируется через GitHub)
- Git установлен на компьютере
- Node.js установлен (для локального тестирования)

---

## 🔧 Шаг 1: Локальное тестирование

**Перед деплоем проверь, что всё работает локально:**

```powershell
# Перейди в папку проекта
cd "D:\dev\coverator v2\coverator_-tsekh-v-labs"

# Установи зависимости (если еще не установлены)
npm install

# Запусти dev сервер
npm run dev
```

**Открой в браузере:** http://localhost:3000

✅ Если всё работает — можно деплоить!

---

## 📦 Шаг 2: Подготовка GitHub репозитория

### 2.1 Создание репозитория на GitHub

1. Зайди на https://github.com/new
2. **Repository name:** `coverator` (или любое другое имя)
3. **Description:** (опционально) "Генератор обложек с эффектами"
4. **Visibility:** выбери **Public** ⚠️ (для бесплатного Vercel нужен публичный репо)
5. **НЕ ставь галочки** на:
   - ❌ Add a README file
   - ❌ Add .gitignore
   - ❌ Choose a license
6. Нажми **Create repository**

### 2.2 Инициализация Git в проекте

Открой PowerShell в папке проекта и выполни:

```powershell
# Перейди в папку проекта
cd "D:\dev\coverator v2\coverator_-tsekh-v-labs"

# Проверь, есть ли уже git репозиторий
git status

# Если ошибка "not a git repository", инициализируй:
git init

# Добавь все файлы (кроме тех, что в .gitignore)
git add .

# Сделай первый коммит
git commit -m "Initial commit: Coverator app"
```

### 2.3 Подключение к GitHub

```powershell
# Добавь remote (замени ТВОЙ_USERNAME на свой GitHub username)
git remote add origin https://github.com/ТВОЙ_USERNAME/coverator.git

# Переименуй ветку в main (если нужно)
git branch -M main

# Отправь код на GitHub
git push -u origin main
```

**Если GitHub попросит авторизацию:**
- Используй Personal Access Token (Settings → Developer settings → Personal access tokens → Tokens (classic))
- Или установи GitHub CLI: `winget install GitHub.cli`

---

## 🌐 Шаг 3: Деплой на Vercel

### 3.1 Регистрация на Vercel

1. Зайди на https://vercel.com
2. Нажми **Sign Up**
3. Выбери **Continue with GitHub** (самый простой способ)
4. Разреши доступ Vercel к твоему GitHub аккаунту

### 3.2 Импорт проекта

1. После входа нажми **Add New...** → **Project**
2. В списке репозиториев найди `coverator` (или как ты назвал)
3. Нажми **Import** рядом с репозиторием

### 3.3 Настройки проекта

Vercel обычно автоматически определяет настройки, но проверь:

**Framework Preset:** `Vite` (должен определиться автоматически)

**Build and Output Settings:**
- **Build Command:** `npm run build` ✅
- **Output Directory:** `dist` ✅
- **Install Command:** `npm install` ✅
- **Root Directory:** `./` (оставь как есть) ✅

**Environment Variables:**
- Для этого проекта пока не нужны (если не используешь API ключи)
- Если понадобятся — добавь в разделе **Environment Variables**

### 3.4 Деплой

1. Нажми кнопку **Deploy** (внизу страницы)
2. Подожди 1-2 минуты (Vercel соберет проект)
3. Когда увидишь ✅ **Success** — готово!

**Твой сайт будет доступен по ссылке:**
- `https://coverator-xxx.vercel.app` (где xxx — случайные символы)
- Или `https://coverator-ТВОЙ_USERNAME.vercel.app` (если настроено)

---

## 🔄 Шаг 4: Автоматические деплои

После первого деплоя всё работает автоматически:

✅ **Каждый `git push` в `main` ветку** → автоматически задеплоит новую версию

✅ **Каждый Pull Request** → Vercel создаст preview деплой (отдельная ссылка для тестирования)

**Как обновить сайт:**
```powershell
# Внеси изменения в код
# Затем:
git add .
git commit -m "Описание изменений"
git push
```

Через 1-2 минуты изменения появятся на сайте!

---

## 📝 Дополнительные настройки (опционально)

### Кастомный домен

1. В Vercel Dashboard выбери проект → **Settings** → **Domains**
2. Нажми **Add Domain**
3. Введи свой домен (например, `coverator.com`)
4. Следуй инструкциям по настройке DNS

### Переменные окружения

Если понадобятся API ключи или другие секреты:

1. **Settings** → **Environment Variables**
2. Добавь переменную:
   - **Name:** `GEMINI_API_KEY` (например)
   - **Value:** твой ключ
   - **Environment:** Production, Preview, Development (выбери нужные)
3. Нажми **Save**
4. Перезадеплой проект (или просто сделай новый push)

---

## ⚠️ Важные моменты

### Что бесплатно на Vercel:

✅ **Публичные репозитории** — полностью бесплатно  
✅ **100GB bandwidth в месяц** — обычно более чем достаточно  
✅ **Неограниченное количество деплоев**  
✅ **Preview деплои для PR**  
✅ **SSL сертификаты** (HTTPS) — автоматически  
✅ **CDN** — автоматически по всему миру  

### Ограничения бесплатного плана:

⚠️ **Serverless Functions** засыпают после 10 секунд бездействия (но для статики это не важно)  
⚠️ **100GB bandwidth в месяц** (если превысишь — нужно будет платить или ограничить трафик)

### Если что-то не работает:

1. **Проверь логи:**
   - Vercel Dashboard → **Deployments** → выбери деплой → **Logs**
   - Там будут все ошибки сборки

2. **Проверь настройки:**
   - Убедись, что `package.json` содержит скрипт `build`
   - Проверь, что `vite.config.ts` настроен правильно
   - Убедись, что все зависимости в `package.json`

3. **Проверь локально:**
   ```powershell
   npm run build
   ```
   Если локально не собирается — на Vercel тоже не соберется

---

## 🎯 Структура проекта для Vercel

Vercel автоматически определит, что это Vite проект, если:
- ✅ Есть `vite.config.ts`
- ✅ В `package.json` есть скрипт `build`
- ✅ В `package.json` есть зависимость `vite`

**Все это уже есть в проекте!** 🎉

---

## 📦 Что происходит при деплое

Vercel выполняет следующие шаги:

1. **`npm install`** — устанавливает все зависимости из `package.json`
2. **`npm run build`** — собирает проект в папку `dist` (Vite создает оптимизированные файлы)
3. **Раздача статики** — Vercel раздает файлы из `dist` через CDN по всему миру

---

## 🔗 Полезные ссылки

- **Vercel Dashboard:** https://vercel.com/dashboard
- **Vercel Docs:** https://vercel.com/docs
- **GitHub:** https://github.com
- **Vite Docs:** https://vitejs.dev

---

## ✅ Чеклист перед деплоем

- [ ] Проект работает локально (`npm run dev`)
- [ ] Проект собирается локально (`npm run build`)
- [ ] Создан GitHub репозиторий (Public)
- [ ] Код залит на GitHub (`git push`)
- [ ] Зарегистрирован на Vercel
- [ ] Проект импортирован в Vercel
- [ ] Настройки проекта проверены
- [ ] Деплой успешно завершен
- [ ] Сайт открывается по ссылке

---

**Готово!** 🎉 После деплоя твой проект будет доступен по ссылке вида `https://coverator-xxx.vercel.app`

**Время деплоя:** ~5-10 минут (включая регистрацию и настройку)

### Шаг 2: Деплой на Vercel

1. **Зарегистрируйся/войди на Vercel:**
   - Зайди на https://vercel.com
   - Нажми **Sign Up** и войди через GitHub (самый простой способ)

2. **Импортируй проект:**
   - После входа нажми **Add New...** → **Project**
   - Выбери репозиторий `coverator` из списка
   - Нажми **Import**

3. **Настройки проекта (важно!):**
   
   **Framework Preset:** Vite (должен определиться автоматически)
   
   **Build Command:** `npm run build`
   
   **Output Directory:** `dist` (стандартная папка для Vite)
   
   **Install Command:** `npm install`
   
   **Root Directory:** `./` (оставь как есть)

4. **Environment Variables (если нужны):**
   - Если проект использует API ключи, добавь их здесь
   - Для этого проекта пока не нужны

5. **Деплой:**
   - Нажми **Deploy**
   - Подожди 1-2 минуты
   - Готово! Получишь ссылку типа `coverator-xxx.vercel.app`

### Шаг 3: Автоматические деплои

После первого деплоя:
- Каждый `git push` в `main` ветку автоматически задеплоит новую версию
- Vercel создаст preview для каждого Pull Request

## 📝 Дополнительные настройки (опционально)

### Кастомный домен

1. В настройках проекта на Vercel → **Settings** → **Domains**
2. Добавь свой домен (если есть)

### Переменные окружения (если понадобятся)

1. **Settings** → **Environment Variables**
2. Добавь переменные (например, `GEMINI_API_KEY`)
3. Перезадеплой проект

## 🔧 Локальный запуск (для тестирования)

```powershell
# Установка зависимостей (если еще не установлены)
npm install

# Запуск dev сервера
npm run dev

# Откроется на http://localhost:3000
```

## ⚠️ Важные моменты

1. **Vercel бесплатен** для:
   - Публичных репозиториев
   - До 100GB bandwidth в месяц
   - Неограниченное количество деплоев

2. **Ограничения бесплатного плана:**
   - Функции засыпают после 10 секунд бездействия (но для статики это не важно)
   - 100GB bandwidth в месяц

3. **Если что-то не работает:**
   - Проверь логи в Vercel Dashboard → **Deployments** → выбери деплой → **Logs**
   - Убедись, что `package.json` содержит правильные скрипты
   - Проверь, что `vite.config.ts` настроен правильно

## 🎯 Структура проекта для Vercel

Vercel автоматически определит, что это Vite проект, если:
- ✅ Есть `vite.config.ts`
- ✅ В `package.json` есть скрипт `build`
- ✅ В `package.json` есть зависимость `vite`

Все это уже есть в проекте!

## 📦 Что будет задеплоено

Vercel запустит:
1. `npm install` - установит зависимости
2. `npm run build` - соберет проект в папку `dist`
3. Раздаст статические файлы из `dist`

## 🔗 Полезные ссылки

- Vercel Dashboard: https://vercel.com/dashboard
- Vercel Docs: https://vercel.com/docs
- GitHub: https://github.com

---

**Готово!** После деплоя твой проект будет доступен по ссылке вида `https://coverator-xxx.vercel.app`

