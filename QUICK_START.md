# ⚡ Быстрый старт Ковератора

## 🏃 Локальный запуск (30 секунд)

```powershell
# 1. Установи зависимости (если еще не установлены)
npm install

# 2. Запусти dev сервер
npm run dev

# 3. Открой в браузере
# http://localhost:3000
```

## 🚀 Деплой на Vercel (5 минут)

### Минимальные шаги:

1. **GitHub:**
   - Создай репозиторий на https://github.com/new (Public!)
   - Выполни в PowerShell:
   ```powershell
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ТВОЙ_USERNAME/coverator.git
   git branch -M main
   git push -u origin main
   ```

2. **Vercel:**
   - Зайди на https://vercel.com
   - Войди через GitHub
   - Add New → Project → выбери `coverator` → Import
   - Нажми Deploy
   - Готово! Сайт будет на `https://coverator-xxx.vercel.app`

**Подробные инструкции:** см. [DEPLOY.md](DEPLOY.md)

---

## 📝 Полезные команды

```powershell
# Локальный запуск
npm run dev

# Сборка для продакшена (проверка перед деплоем)
npm run build

# Просмотр собранной версии локально
npm run preview

# Обновление на Vercel (после изменений)
git add .
git commit -m "Описание изменений"
git push
```

